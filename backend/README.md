# Backend (User Management Dashboard)

## Setup
- `cd backend`
- `npm install`
- `npm run dev` (requires nodemon) or `npm start`

Server runs at `http://localhost:4000` by default.
API base: `http://localhost:4000/api/users`
